-- Criação de protótipo da tabela apenas para testes de interface, não é a tabela final
-- Rodar a query abaixo no seu servidor de banco de dados para que o código main.py funcione corretamente
CREATE TABLE users (
    username TEXT PRIMARY KEY,
    password TEXT NOT NULL
);